from setuptools import setup

setup(name='weirdochat',
      packages=['weirdochat'],
      include_package_data=True,
      install_requires=[
          'PyQt5',
          'QtPy',
          'Quamash',
      ])
